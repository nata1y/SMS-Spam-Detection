from setuptools import setup, find_packages

setup(name='SMS-Spam-Detection',
      version='0.1',
      description="Spam detection tool",
      long_description="",
      author="",
      author_email="",
      install_requires=[],
      packages=find_packages(),
)
